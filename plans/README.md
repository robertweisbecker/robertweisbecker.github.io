# Implementation Plans

Generated by the improve skill on 2026-07-01 against commit `38540a2`; refreshed after pulling `origin/master` to commit `f87f0ad` on 2026-07-01; reconciled after Plan 001 merged to `b72fe64` on 2026-07-01. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Audit Summary

Scope: standard audit of the Next.js App Router personal site in `/Users/robertweisbecker/.codex/worktrees/8797/robertweisbecker.github.io`, then reconciliation after the merged maintainability PR. I read root docs/config, package scripts, the App Router tree, high-churn UI/demo files, private routes, the Letterboxd API path, and representative utilities/components.

Verification observed during audit:

- `npm audit --audit-level=high --omit=dev` exited 0 with no high runtime advisories.
- `./node_modules/.bin/tsc --noEmit --incremental false --pretty false` could not run because dependencies are not installed in this worktree.
- `npm ls --depth=0` showed all manifest dependencies as unmet in this worktree, which means executors should run `npm install` first.
- Production builds were intentionally not run because this improve pass is read-only for source code and `npm run build` writes `.next` artifacts. After the merge, `npm run build` is now the default Turbopack path and `npm run build:webpack` is the fallback.

Verification observed during reconcile (master `b72fe64`, 2026-07-01):

- `npm install` — exit 0.
- `npm run check` — exit 0 (`typecheck`, `lint`, `format:check`).
- `npm audit --audit-level=high --omit=dev` — exit 0, no high runtime advisories.
- Plan 001 scope is fully merged; Plans 002–004 remain open and unchanged by the merge aside from the verification baseline landing.

Not audited: full browser rendering, production Vercel behavior, generated `.next` output, exhaustive dependency freshness, visual regressions across all private prototypes, and every MDX content page line-by-line.

## Vetted Findings

| #   | Finding                                                       | Category                  | Impact                                                                                                                                                                                                | Effort | Risk | Evidence                                                                                                                                         |
| --- | ------------------------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Add a lightweight verification baseline before more refactors | DX                        | This personal portfolio/playground does not need broad tests by default, but lint warnings alone miss TypeScript, route, MDX, and production-build failures.                                          | S      | LOW  | **RESOLVED** on `b72fe64` — `npm run typecheck`, `npm run check`; `types/static-asset-imports.d.ts`; `.prettierrc` / `.prettierignore` alignment |
| 2   | Reduce build time and shared bundle cost                      | performance / DX          | Production builds are high for a personal portfolio because private QA/prototype routes still ship, `/playground` is monolithic, and shared client boundaries pull too much into common route chunks. | L      | MED  | `app/private/**`, `app/playground/page.tsx`, `.next/diagnostics/route-bundle-stats.json` from the merged optimization pass                       |
| 3   | Cache and normalize the Letterboxd feed route                 | performance / correctness | The homepage calls `/api/letterboxd`; each route hit currently parses the remote RSS URL directly and returns loosely typed external URLs/rating values.                                              | M      | MED  | `app/page.tsx:369-380`, `components/demos/letterboxd.tsx:13-72`, `app/api/letterboxd/route.ts:15-93`, `next.config.ts:4-11`                      |
| 4   | Refresh stale repo guidance after route/build/API changes     | docs / DX                 | Agent guidance still points executors at deleted paths and stale route/build facts; after private routes become dev-only and playground splits, docs should describe the final shape.                 | S      | LOW  | `AGENTS.md`, `README.md`, `app/private/qa/page.tsx`, `app/api/letterboxd/route.ts`, `package.json`                                               |

## Direction Options

- The private QA route has enough component coverage to remain a useful local design-system harness, but it should become dev-only instead of shipping in production.
- The public `/playground` route remains a large collection of examples and experiments after the merged cleanup PR. It should stay public, but as a lightweight index plus focused child routes.

## Execution Order & Status

| Plan | Title                                        | Priority | Effort | Depends on    | Status |
| ---- | -------------------------------------------- | -------- | ------ | ------------- | ------ |
| 001  | Add a lightweight verification baseline      | P1       | S      | -             | DONE   |
| 002  | Reduce build and bundle cost                 | P1       | L      | 001           | TODO   |
| 003  | Cache and normalize the Letterboxd API route | P2       | M      | 001           | TODO   |
| 004  | Refresh stale repo guidance                  | P2       | S      | 001, 002, 003 | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- Plan 001 is **DONE** on master (`b72fe64`). Plans 002 and 003 should use `npm run check` before and after implementation.
- Plan 002 still needs its own `typecheck:build` / `analyze:build` scripts; those are separate from Plan 001's repo-wide `typecheck`.
- Plan 004 should run after 002 and 003 so README and AGENTS document the final route/build/API shape, not intermediate states. Plan 004 should also document `npm run typecheck` and `npm run check` once those scripts exist (they do as of `b72fe64`).
- Do not add a test runner by default. Add tests later only for pure logic that repeatedly regresses or becomes hard to verify through typecheck/build/browser QA.

## Reconcile Notes

- Pulled `origin/master` fast-forward from `38540a2` to `f87f0ad`.
- None of the four TODO plans were fixed independently by the merge.
- The plans were refreshed for the PR's build-script change: default production build is now `npm run build` / Turbopack; webpack fallback is `npm run build:webpack`.
- After maintainer feedback, Plan 001 was narrowed from a Vitest/unit-test baseline to a lower-overhead typecheck/check-script baseline appropriate for a personal design-engineering portfolio and playground.
- Added the build/bundle reduction plan from the performance optimization thread and renamed it to `plans/002-reduce-build-and-bundle-cost.md`.
- The earlier robots-only private route plan was superseded: private QA/prototype routes should not ship in production, so noindex metadata alone is the wrong target.
- **Plan 001 executed and merged** (PR #12, squash merge `b72fe64` on master, 2026-07-01): added `typecheck` and `check` scripts. Two blockers surfaced during verification:
  - Standalone `tsc` failed on `@/public/**` image imports while `next build` passed; fixed with `types/static-asset-imports.d.ts`.
  - `format:check` failed on 20 pre-existing files; fixed with a one-time `npm run format` (formatting-only). Follow-up aligned `.prettierrc`, `.prettierignore`, VS Code settings, and pinned `prettier@3.9.4` so format-on-save matches `npm run format:check`.
- **Reconcile after Plan 001 merge** (`b72fe64`, 2026-07-01):
  - `npm run check` passes on master; executors no longer need to bootstrap verification scripts before Plans 002/003.
  - Plans 002–004 drift checks should use `b72fe64` as the baseline commit instead of `f87f0ad`.
  - Finding 1 is resolved; Findings 2–4 are unchanged and still actionable.
  - `README.md` and `AGENTS.md` remain stale (no `typecheck` / `check` docs; deleted `app/components` QA paths; static-export wording; missing Letterboxd API note) — still owned by Plan 004.
  - Private routes still ship in production; `/playground` is still monolithic — still owned by Plan 002.
  - Letterboxd route is still uncached — still owned by Plan 003.

## Findings Considered and Rejected

- `components/code-block.tsx` uses `dangerouslySetInnerHTML` at line 68, but the input is repo-authored MDX/code passed through `sugar-high`; no user-controlled HTML boundary was found.
- `app/sitemap.ts` omits `/private/**`, so the private route issue is not sitemap exposure. The remaining issue is direct routability plus inconsistent robots metadata.
- `plans/005-superseded-private-route-robots-layout.md` proposed central noindex metadata for `/private/**`; this is superseded by Plan 002 because the accepted product decision is to remove private routes from the production route and type-check graph.
- `npm audit --audit-level=high --omit=dev` found no high runtime advisories, so there is no dependency-security plan from this pass.
