import * as React from "react";
import { HStack, Heading, Icon, Tag } from "@chakra-ui/react";

import { ArrowLeftIcon } from "@radix-ui/react-icons";

export const Peek = () => {
	// let history = useHistory();
	return (
		<HStack></HStack>
	);
};
