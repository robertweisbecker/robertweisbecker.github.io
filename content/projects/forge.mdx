export const frontmatter = {
  title: "Everfi Forge",
  subtitle: "Refreshing our internal learning content creation & management app",
  role: "Design Systems Lead",
  date: "Winter - Spring 2017",
  team: [
    { name: "Kevin Grolton-Francisco", role: "Product Manager" },
    { name: "Annie Alvarado", role: "User Research", url: "http://anniealvarado.com/" },
  ],
};

import forgeAfter from "@/public/assets/forge/forge-after.png";
import forgeColorsAll from "@/public/assets/forge/colors-all.png";
import forgeColorsNeutral from "@/public/assets/forge/colors-neutral.png";
import forgeColorsTeal from "@/public/assets/forge/colors-teal.png";
import forgeContrast1 from "@/public/assets/forge/contrast1.png";
import forgeContrast2 from "@/public/assets/forge/contrast2.png";
import forgeButtonModes from "@/public/assets/forge/button-modes.png";
import forgeDark from "@/public/assets/forge/dark.png";
import forgeLight from "@/public/assets/forge/light.png";
import forgeType from "@/public/assets/forge/type.png";
import forgeInputs from "@/public/assets/forge/inputs.png";
import forgeDensityCompare from "@/public/assets/forge/density-compare.png";
import forgeEditActivityBefore from "@/public/assets/forge/edit-activity-before.png";
import forgeEditActivityAfter from "@/public/assets/forge/edit-activity-after.png";
import forgeEditPageBefore from "@/public/assets/forge/edit-page-before.png";
import forgeEditPageAfter from "@/public/assets/forge/edit-page-after.png";
import forgeSidebar from "@/public/assets/forge/sidebar.png";
import forgeButtons from "@/public/assets/forge/buttons.png";
import forgeFields from "@/public/assets/forge/fields.png";
import forgeNav from "@/public/assets/forge/nav.png";
import forgeDialogs from "@/public/assets/forge/dialogs.png";
import forgeWidgets from "@/public/assets/forge/widgets.png";
import forgeThemePicker from "@/public/assets/forge/theme-picker.png";
import forgeLocale from "@/public/assets/forge/locale.png";
import forgeTemplateLogic from "@/public/assets/forge/template-logic.png";
import forgeAppIcon from "@/public/assets/forge/app-icon.png";

<Image src={forgeAfter} alt="The refreshed Forge editor interface" priority />

Forge is the primary tool used by Everfi's implementation team, who translate course designs into interactive e-learning products.

It's a Git-connected Electron app that provides an interface for course creation using the Everfi SDK and component library (aka [Furnace](/furnace)).

Forge, however, started out as a JSON content editing tool with a minimal interface. But the launch of Furnace in early 2020 hastened adoption by the product team, and requests for more powerful WYSIWYG-like features quickly outpaced design and engineering capacity.

This pace of iteration left the app without polish and well-considered UX, and its rough edges began to show. It needed a refresh with strong foundations, taking cues from our other design system initiatives.

---

## Color

<ImageModal src={forgeColorsAll} caption="Full color palette updates" />

In lieu of shadows, increasing lightness can visually communicate a surface as elevated. This is especially important in Forge, given the hierarchical parent-child nature of its editor views.

Just one problem: Forge's styles included only five neutrals whose lightness topped out too quickly, making text virtually illegible after a few child views.

<ImageModal src={forgeColorsNeutral} caption="Neutrals before and after" />

Additional variables that fell outside the color scale were also added over the years, leading to oddities like `$interactive-955`.

<ImageModal src={forgeColorsTeal} caption="Primary colors before and after" />

The new color scale includes values ranging from 100 to 900, plus white and black. Each value falls within a similar lightness range as its siblings, so we can apply colors from different palettes in a more predictable and accessible manner.

Applied to typography and surfaces, the new color palettes fully resolved any contrast concerns:

<LayoutGrid variant="twoUp">
  <ImageModal src={forgeContrast1} caption="Darker darks!" />
  <ImageModal src={forgeContrast2} caption="Lighter lights!" />
</LayoutGrid>

The expanded color system also allowed more nuanced styling: consistent states, additional variants, and a more distinct difference between color modes.

<ImageModal src={forgeButtonModes} />

<ImageToggle before={forgeDark} after={forgeLight} tab1="Dark Mode" tab2="Light Mode" />

## Density

<ImageModal src={forgeType} caption="New type scale, with variable names shifted from HTML references to semantics" />

<ImageModal src={forgeInputs} caption="Dense spacing and type applied to input fields" />

<ImageModal src={forgeDensityCompare} caption="60% shorter (difference in red)" />

## Navigation

A crucial aspect of Forge's utility derives from having its edit views auto-generated by page and component schema.
But organization that makes sense in code doesn't always make sense in a UI.

Edit forms unrelated to content now overtakes the preview. There, you're editing the course itself — not the page you're looking at — free real estate that reinforces the idea of editing a higher-level view.

<ImageToggle before={forgeEditActivityBefore} after={forgeEditActivityAfter} />

This also benefitted performance and usability, since the old edit UI always opened another panel adjacent to the navigation, causing the live preview to resize and force a refresh, losing context.

<ImageToggle before={forgeEditPageBefore} after={forgeEditPageAfter} />

Page actions move from a hidden context menu on list items to a dedicated dropdown on the parent view. No longer split between nav levels, all editing moves to the same pane.

<ImageModal src={forgeSidebar} caption="Before & after of menu hierarchy and actions" />

## Standardization

A look at some components with standardized styling:

<ProjectImageCarousel
  variant="inset"
  slides={[
    { src: forgeButtons, alt: "", caption: "Buttons" },
    { src: forgeFields, alt: "", caption: "Redesigned inputs" },
    { src: forgeNav, alt: "", caption: "Toolbar" },
    { src: forgeDialogs, alt: "", caption: "Dialogs" },
  ]}
/>

## Custom Components

<LayoutGrid variant="twoUp">
  <ImageModal src={forgeWidgets} caption="Utility class widgets" />
  <ImageModal src={forgeThemePicker} caption="Theme picker" />
</LayoutGrid>

<ImageModal src={forgeLocale} caption="In-context locale string editing" />

<ImageModal src={forgeTemplateLogic} caption="Template builder prototypes" />

## App Icon

And, for good measure, I also created app icons for both the production and development builds using our new colors.

<Image src={forgeAppIcon} alt="Forge app icons for production and development builds" />
